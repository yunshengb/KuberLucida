apiVersion: v1
kind: Service
metadata:
  name: asrmaster
  labels:
    name: asrmaster
spec:
  type: LoadBalancer
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    name: asrmaster
